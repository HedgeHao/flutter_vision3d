import 'package:flutter/material.dart';

class StaticUI {
  static const Widget divider = Padding(padding: EdgeInsets.only(top: 8, bottom: 16), child: Divider(height: 1, indent: 10, endIndent: 10));
}
